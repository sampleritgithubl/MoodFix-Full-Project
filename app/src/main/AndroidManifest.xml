<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.innerly.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Innerly"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Innerly">

        <activity
            android:name=".ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.MoodCheckActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.HomeActivity" />
        <activity android:name=".ui.MyGoalsActivity" />
        <activity android:name=".ui.CreateGoalActivity" />
        <activity android:name=".ui.GoalDetailActivity" />
        <activity android:name=".ui.WriteReflectionActivity" />
        <activity android:name=".ui.GoalAchievedActivity" />
        <activity android:name=".ui.ArchivedGoalsActivity" />

    </application>

</manifest>